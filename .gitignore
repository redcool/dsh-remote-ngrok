# ===== 运行时产物 / 大文件 / 私密 —— 不提交 =====
# 本机配置（含 ngrok authtoken、dsh 路径）；提交的是 config.json.temp 模板
config.json

# ngrok 整个目录（exe/日志）
ngrok/

# proxy 依赖（package.json 已声明，clone 后 npm install）
proxy/node_modules/

# proxy 启动时捕获的当前 dsh web 启动 token（每进程随机、dsh 重启即变；内含机密，勿提交）
proxy/dsh_token.txt